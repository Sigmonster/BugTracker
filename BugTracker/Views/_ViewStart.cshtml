
@if (!User.IsInRole("Registered User") && Request.IsAuthenticated)// Add && user role is submitter or higher.
{

    Layout = "~/Views/Shared/_Layout.cshtml";

}
 @if(User.IsInRole("Registered User"))
 {
     Layout = null;
 }
