@model BugTracker.Models.LoginViewModel
@{
    Layout = null;
}
@{
    ViewBag.Title = "Log in";
}
<div class="pull-right">
    <div class="dropdown">
        <button class="btn btn-default btn-outline dropdown-toggle waves-effect waves-light dropdown-toggle" type="button" data-toggle="dropdown" style="border: none;">
            Demo Accounts
            <span class="caret"></span>
        </button>
        <ul class="dropdown-menu">
            <li class="dropdown-header"> </li>
            <li>
                @using (Html.BeginForm("DemoLogin", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form"}))
            {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(m => m.Password, new { @class = "form-control", placeholder = "Username", required = "", Value = "TempPassword12!" })
                    @Html.HiddenFor(m => m.Email, new { @class = "form-control", placeholder = "Username", required = "", Value = "Submitter@gmail.com" })
                    <input id="submitter-login" type="submit" value="Submitter Demo Account" class="" style="display:none;" />
                    <label for="submitter-login" style="width: 100%;" class="fcbtn btn btn-outline btn-success btn-1d">Submitter</label>
                }
            </li>
            <li class="dropdown-header"> </li>
            <li>
                @using (Html.BeginForm("DemoLogin", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form"}))
            {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(m => m.Password, new { @class = "form-control", placeholder = "Username", required = "", Value = "TempPassword12!" })
                    @Html.HiddenFor(m => m.Email, new { @class = "form-control", placeholder = "Username", required = "", Value = "Developer@gmail.com" })
                    <input id="developer-login" type="submit" value="Developer Demo Account" class="" style="display:none;" />
                    <label for="developer-login" style="width: 100%;" class="fcbtn btn btn-outline btn-success btn-1d">Developer</label>

                }
            </li>
            <li class="dropdown-header"> </li>
            <li>
                @using (Html.BeginForm("DemoLogin", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form"}))
            {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(m => m.Password, new { @class = "form-control", placeholder = "Username", required = "", Value = "TempPassword12!" })
                    @Html.HiddenFor(m => m.Email, new { @class = "form-control", placeholder = "Username", required = "", Value = "ProjectManager@gmail.com" })
                    <input id="pm-login" type="submit" value="Project Manager Demo Account" class=""  style="display:none;"/>
                    <label for="pm-login" style="width: 100%;" class="fcbtn btn btn-outline btn-success btn-1d">Project Manager</label>
                }
            </li>
            <li class="dropdown-header"> </li>
            <li>
                @using (Html.BeginForm("DemoLogin", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form"}))
            {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(m => m.Password, new { @class = "form-control", placeholder = "Username", required = "", Value = "TempPassword12!" })
                    @Html.HiddenFor(m => m.Email, new { @class = "form-control", placeholder = "Username", required = "", Value = "Admin@gmail.com" })
                    <input id="admin-login" type="submit" value="Admin Demo Account" class=""  style="display:none;" />
                    <label for="admin-login" style="width: 100%;" class="fcbtn btn btn-outline btn-success btn-1d">Admin</label>
                }
            </li>

        </ul>

    </div>
</div>
@using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form", id = "loginform" }))
{
    @Html.AntiForgeryToken()
    <div class="login-err-box">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    </div>
    <div class="row">
        <div class="pull-left">
            <h3 class="box-title m-b-20">Sign In</h3>
        </div>

    </div>
        <div class="form-group ">
            <div class="col-xs-12">
                @*<input class="form-control" type="text" required="" placeholder="Username">*@
                @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = "Username", required = "" })
                @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
            </div>
        </div>
                    <div class="form-group">
                        <div class="col-xs-12">
                            @*<input class="form-control" type="password" required="" placeholder="Password">*@
                            @Html.PasswordFor(m => m.Password, new { @class = "form-control", required = "", placeholder = "Password" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-xs-12">
                            <div class="checkbox checkbox-primary pull-left p-t-0">
                                @Html.CheckBoxFor(m => m.RememberMe)
                                @Html.LabelFor(m => m.RememberMe, new { @class = "checkbox" })
                            </div>
                            <a href="javascript:void(0)" id="to-recover" class="text-dark pull-right"><i class="fa fa-lock m-r-5"></i> Forgot pwd?</a>
                        </div>
                    </div>
                    <div class="form-group text-center m-t-20">
                        <div class="col-xs-12">
                            <button class="btn btn-info btn-lg btn-block text-uppercase waves-effect waves-light" form="loginform" type="submit">Log In</button>
                        </div>
                    </div>
}
                    <div class="row">
                        @*<div class="col-xs-12 col-sm-12 col-md-12 m-t-10 text-center">
                            <div class="social"><a href="javascript:void(0)" class="btn  btn-facebook" data-toggle="tooltip" title="Login with Facebook"> <i aria-hidden="true" class="fa fa-facebook"></i> </a> <a href="javascript:void(0)" class="btn btn-googleplus" data-toggle="tooltip" title="Login with Google"> <i aria-hidden="true" class="fa fa-google-plus"></i> </a> </div>
                        </div>*@
                    </div>
                    <div class="form-group m-b-0">
                        <div class="col-sm-12 text-center">
                            <p>Don't have an account? <a href="javascript:void(0)" id="go-to-register" class="text-dark pull-right"><i class="fa fa-pencil m-r-5"></i> Sign Up.</a>
                        </div>
                    </div>


